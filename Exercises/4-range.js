'use strict';

const range = (start, end) => {
  const arr = [];
  for (let index = start; index <= end; index++) {
    const element = arr[index];
    return element;
  }
};

module.exports = { range };
